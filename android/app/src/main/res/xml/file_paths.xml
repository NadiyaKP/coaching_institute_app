<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- External storage paths -->
    <external-path 
        name="external_files" 
        path="." />
    
    <!-- Cache directory -->
    <cache-path 
        name="cache" 
        path="." />
    
    <!-- External cache directory -->
    <external-cache-path 
        name="external_cache" 
        path="." />
    
    <!-- Files directory -->
    <files-path 
        name="files" 
        path="." />
    
    <!-- Pictures directory specifically for camera -->
    <external-files-path 
        name="pictures" 
        path="Pictures/" />
</paths>