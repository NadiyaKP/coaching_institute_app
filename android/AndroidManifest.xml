<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.coaching_institute_app">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- Focus Mode Overlay -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- 🔥 CRITICAL: Permission for foreground app detection -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" 
        tools:ignore="ProtectedPermissions" />
    
    <!-- 🔥 ADDED: Required for getRunningTasks on older Android versions -->
    <uses-permission android:name="android.permission.GET_TASKS" 
        android:maxSdkVersion="22" />
    
    <!-- 🔥 ADDED: For better app detection on Android 10+ -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" 
        tools:ignore="QueryAllPackagesPermission" />

    <!-- Optional: Alarm + Battery Optimization -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Camera Permission for Exam Answer Submission -->
    <uses-permission android:name="android.permission.CAMERA" />
    
    <!-- Storage Permissions for Android 12 and below (Legacy) -->
    <!-- These are only used on Android 12 and below, won't affect Play Store policy for Android 13+ -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" 
                     android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" 
                     android:maxSdkVersion="32"
                     tools:ignore="ScopedStorage" />
    
    <!-- Modern Storage Permission for Android 13+ (Required by Play Store) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    
    <!-- Camera Feature Declaration -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <!-- 🔥 ENHANCED: Add query for package usage stats and app detection -->
    <queries>
        <!-- Allows app to detect all launchable applications -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER"/>
        </intent>
        
        <!-- Query for specific popular apps that users might allow -->
        <package android:name="com.whatsapp" />
        <package android:name="com.instagram.android" />
        <package android:name="com.facebook.katana" />
        <package android:name="com.facebook.orca" />
        <package android:name="com.google.android.youtube" />
        <package android:name="com.android.chrome" />
        <package android:name="com.google.android.apps.photos" />
        <package android:name="com.google.android.gm" />
        <package android:name="com.google.android.apps.maps" />
        <package android:name="com.android.settings" />
        <package android:name="com.google.android.calendar" />
        <package android:name="com.google.android.contacts" />
        <package android:name="com.android.vending" />
        <package android:name="com.google.android.apps.docs" />
        <package android:name="com.google.android.apps.messaging" />
        <package android:name="com.android.email" />
        <package android:name="com.android.calculator2" />
        <package android:name="com.android.dialer" />
        <package android:name="com.android.mms" />
        <package android:name="com.android.camera2" />
        <package android:name="com.android.gallery3d" />
        <package android:name="com.spotify.music" />
        <package android:name="com.netflix.mediaclient" />
        <package android:name="com.amazon.mShop.android.shopping" />
        <package android:name="com.microsoft.office.word" />
        <package android:name="com.microsoft.office.excel" />
        <package android:name="com.microsoft.office.powerpoint" />
        <package android:name="com.google.android.apps.meetings" />
        <package android:name="us.zoom.videomeetings" />
        <package android:name="com.skype.raider" />
        <package android:name="com.telegram.messenger" />
        <package android:name="com.snapchat.android" />
        <package android:name="com.twitter.android" />
        <package android:name="com.linkedin.android" />
        <package android:name="com.reddit.frontpage" />
        
        <!-- 🔥 ADDED: For UsageStatsManager access -->
        <intent>
            <action android:name="android.settings.USAGE_ACCESS_SETTINGS" />
        </intent>
        
        <!-- 🔥 ADDED: For overlay permission -->
        <intent>
            <action android:name="android.settings.action.MANAGE_OVERLAY_PERMISSION" />
            <data android:scheme="package" />
        </intent>
    </queries>

    <application
        android:name="${applicationName}"
        android:label="Signature_Institute"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:hardwareAccelerated="true"
        android:supportsRtl="true"
        android:requestLegacyExternalStorage="true"
        android:allowBackup="true"
        android:persistent="true"
        android:largeHeap="true"
        tools:targetApi="31"
        tools:ignore="UnusedAttribute">

        <!-- 🔥 ADDED: Overlay Service for better app monitoring -->
        <service
            android:name=".FocusModeOverlayService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse" />
        
        <!-- 🔥 ADDED: Foreground Service for overlay -->
        <service
            android:name=".FocusModeForegroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <intent-filter>
                <action android:name="com.example.coaching_institute_app.FocusModeForegroundService" />
            </intent-filter>
        </service>

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:screenOrientation="portrait"
            android:resizeableActivity="false"
            android:supportsPictureInPicture="false"
            android:excludeFromRecents="false">

            <!-- Normal Theme -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <!-- App Launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            
            <!-- 🔥 ADDED: Handle deep links for returning to app -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="focusmode" 
                      android:host="return" />
            </intent-filter>
        </activity>

        <!-- Razorpay Payment Activity -->
        <activity
            android:name="com.razorpay.CheckoutActivity"
            android:exported="true"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize"
            android:theme="@style/CheckoutTheme"
            tools:replace="android:exported" />

        <!-- Background Fetch Services -->
        <service
            android:name="com.transistorsoft.flutter.backgroundfetch.HeadlessTask"
            android:exported="false" />

        <service
            android:name="com.transistorsoft.flutter.backgroundfetch.BackgroundFetchService"
            android:exported="false"
            android:permission="android.permission.BIND_JOB_SERVICE" />

        <!-- FileProvider for Camera Images (Required for Android 7.0+) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Flutter Embedding -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
            
        <!-- 🔥 ADDED: For overlay compatibility -->
        <meta-data
            android:name="android.max_aspect"
            android:value="2.1" />
            
        <!-- 🔥 ADDED: For preventing app from being killed -->
        <meta-data
            android:name="android.app.foreground_service_type"
            android:value="specialUse" />

    </application>

</manifest>